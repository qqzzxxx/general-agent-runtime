# Supervisor policy pointer — v2

The active policy is `control/CODEX_SUPERVISOR_RUNTIME.md`.

Fixed route: Python Orchestrator -> codex exec -> root TO_ZCODE.md -> ZCode Desktop Scheduled Automation -> root SUPERVISOR_BRIEF.md + ZCODE_DONE.flag -> Python -> codex exec.

Computer Use / GUI control is not part of the active system and must not be used.
